#!/usr/bin/env sh

user="connorb3"
host="myth.stanford.edu"

mkdir -p ~/.ssh/ctl
ssh -Nf -o ControlMaster=yes -o ControlPath="~/.ssh/ctl/%L-%r@%h:%p" "$user@$host"

echo "Sleeping"
sleep 3

ssh -O exit -o ControlPath="~/.ssh/ctl/%L-%r@%h:%p" "$user@$host"
